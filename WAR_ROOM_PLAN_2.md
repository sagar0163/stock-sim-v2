# WAR_ROOM_PLAN_2

- [x] Create `Leaderboard` mongoose model to cache rankings.
- [x] Implement a function (or aggregation pipeline) to compute and update leaderboard ranks.
- [x] Schedule the leaderboard update function (e.g., using `setInterval`).
- [ ] Refactor `/api/leaderboard` to fetch from the `Leaderboard` collection.
- [ ] Refactor `/api/leaderboard/my-rank` to fetch the specific user's rank from the `Leaderboard` collection.
- [ ] Test the new implementation.
